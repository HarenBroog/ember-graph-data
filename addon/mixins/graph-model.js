import Ember from 'ember'
import extractResponseKey from '../utils/extract-response-key-from-graphql-definition.js'

const { Mixin } = Ember

export default Mixin.create({
  mutate(mutation, variables) {

  }
});
